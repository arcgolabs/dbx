version: '3'

tasks:
  test:
    desc: Run tests for the root module.
    cmds:
      - GOCACHE="$PWD/.cache/go-build" go test ./...

  vet:
    desc: Run go vet for the root module.
    cmds:
      - GOCACHE="$PWD/.cache/go-build" go vet ./...

  test:workspace:
    desc: Run tests for every module in go.work.
    cmds:
      - GOCACHE="$PWD/.cache/go-build" go test ./... ./examples/... ./migrate/... ./sqltmpl/... ./sqltmpl/validate/... ./sqltmpl/validate/mysqlparser/... ./sqltmpl/validate/postgresparser/... ./sqltmpl/validate/sqliteparser/...

  vet:workspace:
    desc: Run go vet for every module in go.work.
    cmds:
      - GOCACHE="$PWD/.cache/go-build" go vet ./... ./examples/... ./migrate/... ./sqltmpl/... ./sqltmpl/validate/... ./sqltmpl/validate/mysqlparser/... ./sqltmpl/validate/postgresparser/... ./sqltmpl/validate/sqliteparser/...

  ci:
    desc: Run workspace test and vet checks.
    cmds:
      - task test:workspace
      - task vet:workspace
